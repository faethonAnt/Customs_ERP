@model List<CustomsERP.Core.Receiver>

<div class="page-header">
    <h1>Receivers</h1>
    <a class="btn btn-primary" asp-controller="Receiver" asp-action="Create">+ New Receiver</a>
</div>

<table class="table table-striped table-hover align-middle">
    <thead>
    <tr>
        <th>Eori</th>
        <th>Name</th>
        <th>Address</th>
        <th>City</th>
        <th>Zip Code</th>
        <th>Country</th>
        <th class="text-end">Actions</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var receiver in Model)
    {
        <tr>
            <td>@receiver.Eori</td>
            <td>@receiver.Name</td>
            <td>@receiver.Address</td>
            <td>@receiver.City</td>
            <td>@receiver.ZipCode</td>
            <td>@receiver.CountryCode</td>
            <td class="text-end table-actions">
                <a class="btn-icon btn-icon-edit" asp-controller="Receiver" asp-action="Update" asp-route-id="@receiver.Id" title="Edit">✎</a>
                <a class="btn-icon btn-icon-delete" asp-controller="Receiver" asp-action="Delete" asp-route-id="@receiver.Id" title="Delete">❌</a>
            </td>
        </tr>
    }
    </tbody>
</table>
